# Block bots searching for vulnerable paths
# Catches requests to wp-admin, phpmyadmin, .well-known exploits, etc.

[nginx-botsearch]
enabled  = true
port     = http,https
filter   = nginx-botsearch
logpath  = /var/log/nginx/access.log
backend  = auto

maxretry = 2
findtime = 600
bantime  = 86400
