# Nginx HTTP basic auth brute-force protection
# Bans IP after 5 failed auth attempts within 10 minutes

[nginx-http-auth]
enabled  = true
port     = http,https
filter   = nginx-http-auth
logpath  = /var/log/nginx/error.log
backend  = auto

maxretry = 5
findtime = 600
bantime  = 3600
